--------------------------------------------------------------------------------------------------------------
-- McSA - 2012 MilchschniTTe - a.k.a MrMilchprodukT @ MTA:SA -------------------------------------------------
--------------------------------------------------------------------------------------------------------------

-- ENGINE --
-- How many blocks should be defined?
-- Do NOT enter more than you really have!
ENGINE_MaxDef = 15

-- How many mobs should be defined?
-- Do NOT enter more than you really have!
ENGINE_MOBS_MaxDef = 1

-- How far should blocks be seen?
ENGINE_LODDistance = 999 -- Units

-- How many Blocks can be placed in an Area?
ENGINE_MaxAreaBlocks = 500 -- Blocks

-- Enable McStreamer? (May slow-down on old Computers | FULL CLIENT-SIDE SCRIPT)
ENGINE_McStreamerEnabled = true


-- PERMISSIONS -- ( Admins arent affected by these settings! )
-- Allow players to protect their blocks?
PERMISSION_AllowProtection = true

-- Allow players to place blocks?
PERMISSION_AllowPlace = true

-- Allow players to destroy blocks?
PERMISSION_AllowDestroy = true


-- MOBS --
-- Spawn-Distance defines a multiplier ( Just leave as-is )
MOBS_SpawnDistance = 30.0

-- Spawnrate
MOBS_Spawnrate = 5 -- 0 to 100 | 0 = No Spawn | 100 = Every Second

-- Hostile Speed Level --
-- 1: Slow | 2: Fast | 3: Super-Fast
MOBS_HSLevel = 2

-- Allow Mobs to destroy Blocks? ( Creeper-Explosion )
MOBS_DestroyBlocks = true

-- Define the strength of a Creeper-Explosion
MOBS_CreeperStrength = 4.0

-- Enable teh kreepah?
MOBS_EnableCreepers = true